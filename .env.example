GITHUB_TOKEN=YOUR-OWN-FINE-GRAIN-TOKEN-SEE-README
GITHUB_OWNER=Your-Github-Nickname
GITHUB_REPO=Your-repo
GITHUB_BRANCH=The-default-branch-for-commit-main-by-default
GITHUB_UPLOAD_BRANCH=notes/upload-develop
GITHUB_TARGET_BRANCH=develop

UPLOAD_MODE=local|remote|both # choose one, by default remote - for dev environment, use local or both, on production use remote

# Image Domain Configuration
# Comma-separated list of allowed domains (empty = allow all)
ALLOWED_IMAGE_DOMAINS=imgur.com,github.com,githubusercontent.com
# Comma-separated list of blocked domains (empty = block none)
BLOCKED_IMAGE_DOMAINS=